# Dependencias de runtime de la API (FastAPI). No modifica el requirements.txt
# raíz, que gobierna el despliegue de Streamlit Cloud.
fastapi>=0.115
uvicorn>=0.30

# Solo necesario en producción cuando TURSO_DATABASE_URL está configurado
# (doble conexión: lectura local + escritura en Turso). En dev/tests, sin
# esta variable, la app usa SQLite local y no requiere esta dependencia.
libsql>=0.1

# Solo para pruebas (TestClient de FastAPI usa httpx), no es dependencia de runtime.
httpx>=0.27
