requests>=2.31
streamlit>=1.30
pandas>=2.0
# chromadb >= 1.5: sus archivos _pb2 son compatibles con protobuf moderno (5/6).
chromadb>=1.5,<2
# El error "Descriptors cannot be created directly" lo causaba la nube al
# resolver versiones viejas en la cadena de protobuf (opentelemetry-proto trae
# _pb2 desactualizados). No se puede forzar PROTOCOL_BUFFERS..._IMPLEMENTATION
# porque el runtime de Streamlit importa protobuf antes que el código de la app.
# Solución: fijar la misma cadena moderna que funciona en local.
protobuf>=6.30,<7
opentelemetry-proto>=1.42
opentelemetry-sdk>=1.42
opentelemetry-api>=1.42
fpdf2>=2.7
Pillow>=10
# Lectura del programa (heat sheet) de campeonatos en PDF (texto nativo).
pdfplumber>=0.11
# Render de la ficha a PNG/PDF. Requiere el navegador: `playwright install chromium`.
# Si no está disponible, la página de Ficha ofrece la descarga en HTML.
playwright>=1.40
# ChromaDB necesita sqlite >= 3.35; en Linux (la nube) se reemplaza el sqlite
# del sistema por esta rueda. En local (mac) no se instala ni hace falta.
pysqlite3-binary>=0.5; platform_system == "Linux"

# Solo para desarrollo/pruebas (no afecta el despliegue):
pytest>=8.0
